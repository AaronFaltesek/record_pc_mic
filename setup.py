from setuptools import setup

setup(
    name='record_pc_mic_tool',
    version='1.0',
    descript='Tool to record audio through my pc mic on windows 10',
    py_modules=['record_pc_mic_tool'],
    install_requires=[''] #external packages as dependencies
)